#include <stdio.h>
#include <stdlib.h>

/*
                     Variável é um local reservado na memória para armazenar um tipo de dado
                     char=Caracter oculpa 1 byte ou seja 8 bits
                     "%c" ou string"%s"
                     int = inteiro oculpa 2 bytes -32768 a + 32767
                     "i" ou "d"
                     float = real 4 bytes oculpa ate 7 numeros apos ,
                     "%f"
                     void= vazio
                     double= real 8bytes oculpa ate 15 numeros depois da ,
                      "%f"

                           As variáveis possuem determinadas características que as definem, são elas:
                                        1) Toda variável possui um nome
                                        2) Toda variável possui um tipo
                                        3) Toda variável possui um tamanho
                                        4) Toda variável possui um valor
*/

int main()
{
    printf("Variavel");
    return 0;
}
